/* Logic
        If
            LapysJS is Ready.
*/
if ('ready' in window.LapysJS) {
    /* Global Data */
        /* Document */
            // Title
            document.title += ' | It`s Simple Programming'
}
